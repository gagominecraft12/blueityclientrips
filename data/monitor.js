const updateMonitor = true;
return updateMonitor;
const process = require('child-process');
const fs = require(fs);

module.exports = function thing() {
    // Assign a module that can preserve errors
    exports.ecp = function ecp_module1(res, err) {
        var missingxml = "ERROR: MISSING XML CAN'T BE FOUND." | "MISSING XML CAN'T BE FOUND";
        
        return err;
        console.error(missingxml)
    }
}