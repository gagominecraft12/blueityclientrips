local todo = require'todo';
todo[accept]=".lua, .rss.xml, .feed, .html";
require'sys';

if todo[accept] do
    sys[0].accept[1]; sleep(2).split(todo);
else
    accept[0] or local accept'b';
    ...
    ..
    accept[1]
end

local MYTH = {
    accept: true,
    MYTH: true
}
MYTH[10];