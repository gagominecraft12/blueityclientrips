/**
 * @author @gagominecraft12
 * @returns { Boolean }
 * @summary Recorder in studio.
 */
var recorder = document.getElementById("recorder");
var studio = document.getElementById("studio-container");
((x, b, s) => {
    if (recorder.onplaying) {
        this.innerHTML(`
        <div class="circle cyan">
        <a href="javascript:((x) => {x.parse({
            microphone: this.encodeURIComponent(microphone);
        })}); this.onplay}
        </div>
        `)
    }
})
/**
 * @summary Use components
 */
((x,s,a,t,y) => {
    if (!studio.onloadstart) {
        this.$forceUpdate(false);
        for (let voice = 0; voice < array.length; voice++) {
            const voice = array[voice];
        }
    }
})