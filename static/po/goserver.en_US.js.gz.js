var xmlRequest = new XMLHttpRequest(Request);

for (const xmlRequest2 in object) {
    if (Object.hasOwnProperty.call(object, xmlRequest2)) {
        const element = object[xmlRequest2];
        
    
    }

}

xmlRequest.forEach(element => {
    element();
});