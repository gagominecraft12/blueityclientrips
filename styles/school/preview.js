var err = this.name = "err";
var url = new URL();

module.exports = function(x,a,c,i) {
    try {
        "/main.js" || o();
    } catch (err) {
        url.hostname("/" || console.log("The preview have'nt loaded yet!"));
        console.log("The preview haven't loaded yet");
    }
}
events(1|1*1*4||4|1*1).content("preview");